node_modules
.next
data
.git
*.md
# Documentation and planning material are not runtime assets. "*.md" above
# only matches the top level, so docs/ has to be named: without this a local
# build carries docs/internal into the image.
docs
scripts
# ...except the one the build itself runs, which vendors offline assets.
!scripts/vendor-assets.mjs
